# This file contains the Gateway API CRDs
# You can download the latest from: https://github.com/kubernetes-sigs/gateway-api/releases
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: gatewayclasses.gateway.networking.k8s.io
spec:
  group: gateway.networking.k8s.io
  names:
    categories:
    - gateway-api
    kind: GatewayClass
    listKind: GatewayClassList
    plural: gatewayclasses
    singular: gatewayclass
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: GatewayClass describes a class of Gateways available to the user
          for creating Gateway resources.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of GatewayClass.
            properties:
              controllerName:
                description: 'ControllerName is the name of the controller that is
                  managing Gateways of this class. The value of this field MUST be
                  a domain-prefixed path no more than 253 characters in length, e.g.
                  "acme.io/gateway-controller". This field may not be empty.'
                maxLength: 253
                minLength: 1
                type: string
              description:
                description: 'Description helps describe a GatewayClass with more
                  details.'
                maxLength: 1024
                type: string
              parametersRef:
                description: 'ParametersRef is a reference to a resource that contains
                  the configuration parameters corresponding to the GatewayClass.
                  This is optional if the controller does not require any additional
                  configuration.
                  ParametersRef can reference a standard Kubernetes resource, i.e.
                  ConfigMap, or an implementation-specific custom resource. The resource
                  may be in any namespace in the cluster.
                  It is invalid to reference a parametersRef resource that does not
                  exist. It is possible to update the parametersRef to a resource
                  that is syntactically valid but does not exist in the cluster. In
                  such a case, the GatewayClass MUST be accepted and the missing parameters
                  reference MUST be raised to the user.
                  Support: Implementation-specific'
                properties:
                  group:
                    description: Group is the group of the referent.
                    maxLength: 253
                    minLength: 1
                    type: string
                  kind:
                    description: Kind is kind of the referent.
                    maxLength: 253
                    minLength: 1
                    type: string
                  name:
                    description: Name is the name of the referent.
                    maxLength: 253
                    minLength: 1
                    type: string
                  namespace:
                    description: 'Namespace is the namespace of the referent. This
                      field is required when referring to a Namespace-scoped resource
                      and omitted when referring to a Cluster-scoped resource.'
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - group
                - kind
                - name
                type: object
            required:
            - controllerName
            type: object
          status:
            description: Status defines the current state of GatewayClass.
            properties:
              conditions:
                description: "Conditions is the current status from the controller
                  for this GatewayClass. \n The current condition values and meanings
                  for GatewayClass are: \n - \"Accepted: true\" indicates the GatewayClass
                  has been accepted by the controller. \n - \"Accepted: false\" indicates
                  the GatewayClass was not accepted by the controller. The condition
                  Reason and Message should indicate why. \n Controllers should prefer
                  to use the values of SupportedVersion and AllowedRouteKinds in the
                  status to indicate acceptance of a GatewayClass. \n Controllers
                  should only set `Accepted` conditions when a GatewayClass is not
                  accepted due to an error that cannot be represented by SupportedVersion
                  or AllowedRouteKinds."
                items:
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another. This should be when
                        the underlying condition changed.  If that is not known, then
                        using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition. This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon. For instance, if .metadata.generation
                        is currently 12, but the .status.conditions[x].observedGeneration
                        is 9, the condition is out of date with respect to the current
                        state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition. Producers
                        of specific condition types may define expected values and
                        meanings for these arguments, and whether the values are considered
                        a guaranteed API. The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      minLength: 1
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 8
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
